name: Build OpenWrt testing for GL-MT6000

permissions:
  contents: write
  actions: write

on:
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/_build_openwrt.yml
    with:
      config_file: mt6000_testing.config
      check_runner: ubuntu-slim
      build_runner: ubuntu-24.04
      install_deps: true
    permissions:
      contents: write
      actions: write
